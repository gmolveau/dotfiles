#!/usr/bin/env bash
set -o errexit
set -o pipefail
set -o nounset

# ffind = file-find

! command -v "fzf" &> /dev/null && (
    echo "fzf is not installed"
    echo "please install with : "
    echo "$ brew install fzf"
    echo "$ apt-get install fzf"
    exit 1
)
find . -type f | fzf
