#!/usr/bin/env bash
set -o errexit
set -o pipefail
set -o nounset

# print the files and lines where a text is found in current dir
# usage: findtext <text>   (quote multi-word patterns)

if [ $# -eq 0 ]; then
    echo "usage: findtext <text>"
    exit 1
fi

grep -rn "." -e "$1"
